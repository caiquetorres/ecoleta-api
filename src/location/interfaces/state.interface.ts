/**
 * Interface that handles all the state data.
 */
export interface IState {
  /**
   * Property that defines the state name.
   */
  name: string

  /**
   * Property that defines the state iso.
   */
  iso2: string
}
