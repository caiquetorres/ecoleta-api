/**
 * Interface that handles all the country data.
 */
export interface ICountry {
  /**
   * Property that defines the country name.
   */
  name: string

  /**
   * Property that defines the country iso.
   */
  iso2: string
}
